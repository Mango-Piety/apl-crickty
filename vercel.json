{
  "version": 2,
  "installCommand": "echo 'Skipping install because this is a mocked zero-dependency project'",
  "buildCommand": "echo 'Skipping build'",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/public/$1" }
  ]
}
